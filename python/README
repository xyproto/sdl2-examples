First install sdl2.

Install PySDL2. You can do this with your package manager (might be named "python-pysdl2") or with easy_install:

easy_install PySDL2

(might need root access)

Then you should be ready to go.

More information about getting started with PySDL2:

http://pysdl2.readthedocs.org/en/latest/
